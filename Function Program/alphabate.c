#include <stdio.h>
void alpha()
{
    char chara='c';
    // printf("\nEnter the character:");
    // scanf("%c", &chara);
    if (chara == 'a' && chara == 'z')
    {
        printf("it is a Alphabate:");
    }
    else
    {
        printf("It is not a Alphabate:");
    }
}
int main()
{
    alpha();
}
